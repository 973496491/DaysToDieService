package com.loko.utils.config

object CacheModel {

    const val EMPTY_ID = "0"

    object Config {
        private const val KEY = "config/"

        const val KEY_WHITELIST = "${KEY}whitelist/"

        const val KEY_XML_CONTENT = "${KEY}key_xml_content/"
    }
}